{
  "name": "worker-two",
  "version": "0.0.0",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230419.0",
    "typescript": "^5.0.4",
    "wrangler": "2.19.0"
  },
  "private": true,
  "scripts": {
    "start": "wrangler dev --env=dev --local",
    "deploy": "wrangler publish --outdir=dist --env=dev"
  }
}
